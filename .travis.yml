language: node_js
node_js: 
  - "6.11"
install:
  - npm install
script:
  - npm run lint
  - npm run build
deploy:
  local_dir: dist
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master